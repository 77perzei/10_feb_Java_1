const barkisweight = 550
const veteLangd = 700

const identical = barkisweight == veteLangd
const identicalValueAndType = (barkisweight === veteLangd)
const isDifferent = barkisweight != veteLangd  //! jämför värden
const isDifferentValueAndType = barkisweight !== veteLangd // jämför barkis och vete med datatypen 
const isGreater = barkisweight > veteLangd // är värdet större än...
const isLess = barkisweight < veteLangd // Är värdet mindre än...


function ComperisonOperators() {
    alert(isLess)
}